# StratGameConstructor
Tile constructor for hexagon based strategy game

A small program that imports objects, allows for adding, positioning and scaling, and then exports them to a text file in XML format.
